- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: files/tmux.conf
    ~/.vimrc: files/vimrc

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
